import React from "react";
import { Container, NotificationWindow } from "@/components";

const Page = () => {
  return (
    <Container
    className="w-full"
    >
    <NotificationWindow/>
    </Container>
  );
};

export default Page;
