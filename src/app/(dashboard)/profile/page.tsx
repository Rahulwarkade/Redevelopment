import React from "react";
import { Container, ProfileWindow } from "@/components";

const Page = () => {
  return (
    <Container
    className="w-full"
    >
    <ProfileWindow/>
    </Container>
  );
};

export default Page;
