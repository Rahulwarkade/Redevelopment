import React from "react";
import { Container, ProfileWindow } from "@/components";

const Page = () => {
  return (
    <Container
    className="w-full h-full "
    >
    <ProfileWindow/>
    </Container>
  );
};

export default Page;
