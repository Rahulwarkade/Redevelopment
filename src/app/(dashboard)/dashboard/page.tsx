import React from "react";
import { Container, FeedWindow } from "@/components";

const Page = () => {
  return (
    <Container
    className="w-full h-full"
    >
    <FeedWindow/>
    </Container>
  );
};

export default Page;
