import React from "react";
import { Container, FeedWindow } from "@/components";

const Page = () => {
  return (
    <Container
    className="w-full"
    >
    <FeedWindow/>
    </Container>
  );
};

export default Page;
